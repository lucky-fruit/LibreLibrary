# LibreLibrary

To run client:

    cd client
    npm run dev

    Frontend will be running on http://localhost:5173/

To run server:

    cd server
    npm run dev

    Backend will be running on localhost:5000 (default)
